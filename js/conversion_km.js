function km (){

    var m = parseFloat(document.conversion.metros.value)
    var kilometros = 1000;
       document.conversion.m.value=m;
       document.convercion.kilometros.value=kilometros;
       document.conversion.metr.value=m/kilometros;
       //document.Form.resultado.value=a1+a2;











}